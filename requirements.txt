# Core libraries
numpy>=1.24
pandas>=1.5
scikit-learn>=1.2
joblib>=1.2
imbalanced-learn>=0.10

# Plotting / reporting
matplotlib>=3.7
seaborn>=0.12
weasyprint>=58.0           # optional; requires system dependencies (cairo, pango, gdk-pixbuf)
# wkhtmltopdf is an alternative (install via OS package manager)

# GUI & UI
customtkinter>=6.0         # CustomTkinter (Tkinter-based modern UI)
# PyQt5 kept for compatibility if needed
PyQt5>=5.15.11

# Scanner / parsing helpers
python-libnmap==0.7.3
lxml>=4.9                   # robust XML parsing
beautifulsoup4>=4.12        # optional HTML parsing

# HTTP / utils
requests>=2.28
tqdm>=4.65

# Development / testing (optional)
pytest>=7.0

# Notes:
# - After pip install, some packages (weasyprint) require system libraries (cairo, pango, gdk-pixbuf).
# - For wkhtmltopdf-based PDF export install wkhtmltopdf via apt / yum and not via pip.
