# Core libraries
numpy>=1.24
pandas>=1.5
scikit-learn>=1.2
joblib>=1.2
imbalanced-learn>=0.10

# Plotting / reporting
matplotlib>=3.7
seaborn>=0.12

# PDF export (optional; requires system libs)
weasyprint>=58.0       # requires system packages: cairo, pango, gdk-pixbuf
# Alternative: install wkhtmltopdf using system package manager (apt/yum)

# GUI
customtkinter>=6.0
# Optional fallback / compatibility
PyQt5>=5.15.11

# Scanner / parsing helpers
python-libnmap==0.7.3
lxml>=4.9
beautifulsoup4>=4.12

# HTTP / utilities
requests>=2.28
tqdm>=4.65

# Model serving / joblib
joblib>=1.2

# Development / testing (optional)
pytest>=7.0

# Notes:
# - WeasyPrint requires system libraries; install via your distro package manager:
#   sudo apt install libcairo2 libpango-1.0-0 libgdk-pixbuf2.0-0
# - For wkhtmltopdf install via apt: sudo apt install wkhtmltopdf
# - Use a Python 3.9+ virtual environment.
